// Facts only Sohayb can supply. `null` = not yet provided: the site omits the fragment,
// the bot answers 404 + contact card. Fill and rebuild; nothing else needs to change.
export const ownerTodo = {
  juniorsMentored: null as number | null,
  bscExpectedYear: null as number | null,
  saratovYears: null as string | null,
  noticePeriod: null as string | null,
  remotePreference: null as string | null,
};
